target: .cursor/rules/vodou-policy.mdc
blocks: 50,70
frontmatter: |
  ---
  description: Vodou project policy — always applied (generated from templates/rules; edit there)
  globs:
  alwaysApply: true
  ---
---
# Vodou policy (always applied)

This is the **resident** subset of the project policy — the rules that must apply before you think to look anything up. It is `alwaysApply`, so it rides **every prompt** — which is why it carries two blocks, not nine: the parallel-session commit rules and the lane canon, the two `.cursorrules` went three months without. Measured 2026-08-27: four blocks cost 7.5 KB per prompt; two cost ~4 KB. The full set is in `.cursorrules`; the manual is `AGENTS.md`.
