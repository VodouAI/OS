# TOOLS.md - Local Notes

Project-specific setup. Add things like:

- Package manager (npm, yarn, cargo, etc.)
- Test paths and commands
- Build commands
- SSH hosts, device nicknames
- Environment-specific config

## Examples

```markdown
### Build
- `cargo build` / `npm run build`

### Test
- `cargo test` / `npm test`
```
